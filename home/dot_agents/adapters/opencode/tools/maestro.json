{
  "name": "maestro",
  "description": "Executor for planning and dry-run simulation of local pipelines.",
  "mode": "dry-run-only",
  "input_schema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["plan"]
      },
      "pipeline": {
        "type": "string",
        "description": "Human-readable description of the pipeline to simulate."
      }
    },
    "required": ["action", "pipeline"]
  },
  "constraints": {
    "allow_shell": false,
    "allow_file_write": false,
    "allow_network": false
  }
}
