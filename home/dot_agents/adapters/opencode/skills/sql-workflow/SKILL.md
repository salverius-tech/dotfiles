---
name: sql-workflow
description: "SQL workflow guidelines. Activate when working with SQL files (.sql), database queries, migrations, or SQLFluff."
---

Read `~/.agents/skills/sql-workflow/knowledge.md` for the full guidelines. Note: the path is `~/.agents`, not `~/.claude`.
