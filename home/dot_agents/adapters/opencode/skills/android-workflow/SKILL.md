---
name: android-workflow
description: "Android application workflow guidelines. Activate when working with Android projects, Gradle files, Kotlin/Java Android code, Jetpack Compose, AndroidManifest.xml, adb, emulator/device workflows, or Android build/test/lint issues."
---

Read `~/.agents/skills/android-workflow/knowledge.md` for the full guidelines. Note: the path is `~/.agents`, not `~/.claude`.
