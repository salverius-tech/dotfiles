---
name: introspection
description: "Meta-improvement patterns for analyzing interactions and improving rulesets."
---

Read `~/.agents/skills/introspection/knowledge.md` for the full guidelines. Note: the path is `~/.agents`, not `~/.claude`.

## OpenCode-Specific: Session Access

OpenCode session data access mechanisms are runtime-dependent. Consult OpenCode documentation for current session storage and history retrieval methods.
