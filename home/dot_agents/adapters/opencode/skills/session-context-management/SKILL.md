---
name: session-context-management
description: Maintain "just enough" context across work sessions using CURRENT.md, STATUS.md, and LESSONS.md files.
---

Read `~/.agents/skills/session-context-management/knowledge.md` for the full guidelines. Note: the path is `~/.agents`, not `~/.claude`.

## OpenCode-Specific: Instance/Session ID Detection

OpenCode session and instance detection mechanisms are runtime-dependent. Consult OpenCode documentation for current session identification methods.

## OpenCode-Specific: Git Tracking Policy Check

In Activation Instructions step 1, check for `enable-session-commits: true` in the project's `AGENTS.md` or equivalent project configuration file.
