---
name: ansible-workflow
description: Ansible automation workflow guidelines. Activate when working with Ansible playbooks, ansible-playbook, inventory files (.yml, .ini), or Ansible-specific patterns.
---

Read `~/.agents/skills/ansible-workflow/knowledge.md` for the full guidelines. Note: the path is `~/.agents`, not `~/.claude`.

**Note:** For project-specific Ansible patterns, check `.claude/CLAUDE.md` in the project directory.
