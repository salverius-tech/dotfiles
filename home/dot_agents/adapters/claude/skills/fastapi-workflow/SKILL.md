---
name: fastapi-workflow
description: FastAPI framework workflow guidelines. Activate when working with FastAPI projects, uvicorn, or FastAPI-specific patterns.
---

Read `~/.agents/skills/fastapi-workflow/knowledge.md` for the full guidelines. Note: the path is `~/.agents`, not `~/.claude`.

**Note:** For project-specific FastAPI patterns, check `.claude/CLAUDE.md` in the project directory.
