---
name: security-first-design
description: Five-phase security analysis framework. Activate when working with authentication, authorization, API security, sensitive data, secrets management, encryption, or security-critical features.
---

**Auto-activate when:** User mentions authentication, authorization, API security, sensitive data, user input, secrets management, encryption, security review, or when working with `.env$`, `credentials.json`, `secrets.yaml`, API keys, or security-critical features.

Read `~/.agents/skills/security-first-design/knowledge.md` for the full guidelines. Note: the path is `~/.agents`, not `~/.claude`.
