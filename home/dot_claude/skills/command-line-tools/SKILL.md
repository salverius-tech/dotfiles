---
name: command-line-tools
description: Modern CLI tools reference (ripgrep, fd, bat, eza, fzf, zoxide, jq, btop, tldr). Activate when searching files, viewing contents, navigating directories, monitoring resources, or processing JSON.
---

**Auto-activate when:** Searching files or content, viewing file contents, navigating directories, monitoring system resources, processing JSON data, or looking up command documentation.

Read `~/.agents/skills/command-line-tools/knowledge.md` for the full guidelines. Note: the path is `~/.agents`, not `~/.claude`.
