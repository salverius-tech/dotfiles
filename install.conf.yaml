- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.bashrc: .bashrc
    ~/.bash_aliases: .bash_aliases
    ~/files/starship.toml: .config/starship.toml
#    ~/.gitconfig: .gitconfig
