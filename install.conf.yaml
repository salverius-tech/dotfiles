- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.bashrc: .bashrc
    ~/.bash_aliases: .bash_aliases
    ~/repos:
        create: true
    ~/.config/starship.toml:
        create: true
        path: files/starship.toml
    ~/.gitconfig: .gitconfig

- shell:
    - [just initialize-localhost]